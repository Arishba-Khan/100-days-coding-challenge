let buddies = ["Haram", "Zaviyar", "Aidam"];

// Intentional error: Arrays are zero-indexed, so index 3 is out of bounds.
console.log(buddies[3]);

// Correcting the error by accessing a valid index.
console.log(buddies[2]);
